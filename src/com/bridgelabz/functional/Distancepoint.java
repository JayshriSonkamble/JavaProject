/******************************************************************************
 *  Compilation:  javac -d bin DistancePoint.java
 *  Execution:    java -cp bin com.bridgelabz.functional
 *  
 *  Purpose:	calculate the distance from two point using Euclidean distance.
 *@author  Jayshri
 * @version 1.0
 * @since   29-07-2019
 *
 ******************************************************************************/
package com.bridgelabz.functional;

import com.bridgelabz.utility.Utility;

public class Distancepoint {

public static void main(String[] args)
{
		Utility utility=new Utility();
		int x,y;
		double distance = 0;
		
		Utility.distancepoint(args, distance);
		
		
		/*
		 * x =Integer.parseInt(args[0]); y =Integer.parseInt(args[1]);
		 * 
		 * 
		 * distance =Math.sqrt((Math.pow(x*y, x)+Math.pow(y*y, y)));
		 * 
		 * System.out.println("distance from the point (" + x + ", " + y +
		 * ") to origin(0, 0) = " + distance);
		 */


	}

	}

